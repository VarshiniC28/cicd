name: Inputs demo
on: 
  workflow_dispatch: #if we hav to use inputs the trigger should be manual trigger
    inputs:
      environment: #environment variable name here is environment
        description: Choose the environment to deploy
        required: true
        default: dev
jobs:
  input-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo "Deploying the applocation to ${{ inputs.environment }}"
